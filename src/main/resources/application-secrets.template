# Copy this to application-secrets.properties (local) or set as system env vars (prod)
# REQUIRED
DB_USERNAME=nguyendd
DB_PASSWORD=your_password_here
JWT_SECRET_KEY=generate_a_256bit_random_key_here

# OPTIONAL (use defaults if not set)
WEBPAY_PRODUCTION=false
